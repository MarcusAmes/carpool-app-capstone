const initState = {
  id: "",
  email: "",
  businessId: ""
}

const userReducer = (state = initState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default userReducer;